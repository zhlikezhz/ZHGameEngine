add_subdirectory(Empty)
IF(${WIN32})
    add_subdirectory(Windows)
ELSEIF(${UNIX})
    IF(${APPLE})
        add_subdirectory(MacOS)
    ELSE(${APPLE})
        add_subdirectory(Linux)
    ENDIF(${APPLE})
ENDIF(${WIN32})